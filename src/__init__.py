"""liposome-detect: physics-calibrated confocal simulator + liposome puncta detector.

This stage ships the forward simulator (`src.simulator`) and the joint
multi-sample calibration pipeline (`src.calibration`). Training / detector
code is intentionally not yet ported.
"""
